## Server (local dev)
# PORT=4000
# OPENAI_API_KEY=sk-...
#
## Chat model selection (optional)
# CHAT_MODEL=gpt-5.2
# CHAT_MODELS=gpt-5.2,gpt-4.1,gpt-4o-mini
#
## Paywall enforcement (optional, set to 0 to disable limits)
# PAYWALL_ENFORCED=1
#
## Temporary auth (client sends `x-nexus-user-id`)
# No config needed
#
## Categorization
# Force exactly 1 category per indexed screenshot (default is 1)
# MAX_CATEGORIES_PER_DOC=1
#
## RDS (MySQL)
# DB_HOST=tdp-db.cxstpaskcliu.us-east-1.rds.amazonaws.com
# DB_PORT=3306
# DB_USER=your_db_user
# DB_PASSWORD=your_db_password
# DB_NAME=nexus
#
# Set to 1 to auto-create tables on startup (dev only)
# DB_AUTO_MIGRATE=1
#
# Default is enabled. Set to 0 to disable auto ensure.
# DB_ENSURE_SCHEMA=0
#
## S3 (private bucket; server generates presigned URLs)
# AWS_REGION=us-east-1
# S3_BUCKET=nexus-prod-media
#
# Local credentials (dev only). In production, use an IAM role for the server.
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...
# AWS_SESSION_TOKEN=...
